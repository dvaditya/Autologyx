import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    content: {
        padding: '2rem 24px',
        textAlign: 'center'
    },
    footer: {
        display: 'flex',
        justifyContent: 'space-between'
    }
}));
