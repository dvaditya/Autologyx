import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  subHeader: {
    backgroundColor: '#eee',
    color: '#002c5f',
    padding: '10px 24px',
    textTransform: 'uppercase',
    fontSize: 14,
    fontWeight: 600,
  },
}));
